# Base URL - override with env BASE_URL if needed
base.url=https://www.saucedemo.com

# Default credentials - override with env if needed
default.username=standard_user
default.password=secret_sauce

# Timeouts (milliseconds)
implicit.wait.ms=5000
explicit.wait.timeout.sec=10
page.load.timeout.sec=30
